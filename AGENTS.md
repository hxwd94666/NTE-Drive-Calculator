# NTE Drive Calc 仓库开发契约

本文件只保留当前产品硬契约、架构边界和工程门禁。功能细节见 `docs/features.md`，外部能力见
`docs/integrations.md`，未完成能力见 `docs/roadmap.md`，公式和字段见 `docs/reference/`，实机验收见
`docs/validation/windows.md`。本文必须可公开：不得记录账号、Token、CDK、私有地址、真实 UID、用户绝对路径、
完整抓包、OCR 全文、截图或未脱敏日志。

## 1. 事实源与文档维护

冲突时依次采用：已通过的公共行为测试与数据库约束、本文件、架构/功能/集成文档、公开 contract、现有 UI。
未完成能力只写路线图；当前能力只写功能文档；一个事实只保留一个权威位置，其余文档链接引用。

更新文档必须覆盖式重写相关章节：删除失效结论、重复说明、阶段流水账和完成计划，不在文末追加“本次修改”
之类补丁段落。文档不得成为实现细节、个人环境或测试输出的归档位置。

## 2. 开发与完成定义

开始前明确输入/输出所属数据域，冻结的账号、generation、snapshot、静态 dataset、配置版本、`slot_id`、
锁快照和取消 token，以及失败回滚与外部副作用确认方式。产品语义变化按下列顺序完成：

```text
公共行为测试 → contract/文档 → Domain/Service → DAO/Integration
→ Controller/UI → 迁移调用方并删除旧入口 → 专项验证 → core/full 与静态检查
```

缺陷先复现后做最小业务修复。新旧语义不得长期双读、双写或由 UI 分流。schema、payload、错误码、外部协议
变更必须覆盖新库、旧库升级、失败回滚和修复后重试。完成时必须有行为/生命周期测试、当前文档、无新增兼容
门面或跨 Feature 私有调用，并区分新增失败与既有失败。

## 3. 技术基线与分层

- Windows 10/11、Python 3.11、PySide6；入口为 `main.py`，组合根为 `src/ui/app.py`。
- 依赖唯一来源为 `pyproject.toml` 与 `uv.lock`；版本唯一来源为 `src/app/version.py::__version__`。
- 质量入口为 `tools/quality/run_tests.py`：`core` 覆盖关键边界，`full` 执行完整发现。

```text
UI Page/View → Controller → Application Service → Domain/Optimizer 或 DAO/Integration
```

UI 只拥有控件和可丢弃展示状态；Controller 只拥有 worker、取消与投影；Service 冻结请求并编排事务；
Domain/Optimizer 只做不可变纯计算；DAO 独占 schema/SQL/事务；Integration 独占进程、文件、协议、OCR 和输入。
Service、Domain、DAO、Integration 不得导入 `src.features` 或寻找 MainWindow。`AppContext`、
`EquipmentPresentation` 与全局热键只在组合根创建并显式注入。

跨层关系一律使用正式 `character_id`、`item_id`、`suit_id`、`shape_id`、`property_id`、装备 UID 和 `slot_id`；
中文名与 `slot_name` 仅展示。导航使用 key/`parent_key`，不以页号或 MainWindow 字段扫描定位功能。

## 4. 上下文、异步与数据域

`AppContext` 是路径和账号状态唯一组合根：`ApplicationPaths` 管发行资源与应用路径，`AccountContext` 管当前账号，
`generation` 标识账号代次。切换账号顺序固定为：停止账号任务 → 替换上下文并递增 generation → 重建窄服务 →
清空页面缓存 → 恢复允许自动运行的服务。

长任务创建时冻结账号、generation、路径、快照、静态 dataset、配置和目标槽位；回调与写入前再次核对，失效结果
静默丢弃。取消只使本次 token 失效；退出通过公开 `close/stop` 关闭 owner，不修改 worker 私有状态。

| 数据域 | 路径/职责 |
| --- | --- |
| 发行静态 | `data/game_static.sqlite3`；官方目录、成长、技能、敌人、离线权重和毕业模板；运行时只读。 |
| 应用全局 | `config/global_ui_preferences.json`、`config/workshop_weight_template.json`；主题与公开工坊模板。 |
| 账号私有 | `accounts/<account_id>/user_data.sqlite3`；快照、角色、偏好、槽位、方案、锁、任务、战报。 |
| 公共共享 | `data/app_shared.sqlite3`；仅保留旧迁移数据，不覆盖官方静态事实。 |

读取优先级为账号显式配置 → 允许共享的数据 → 发行默认。主题只属应用全局；新安装默认黑色主题，显式旧主题才迁移。
当前发行静态为 v31，代码与候选为 v32；静态或账号迁移只追加，已发布迁移不得改义。

## 5. 静态库、快照与来源能力

静态库只由 `tools/game_data` 在 `build/` 生成候选。候选完成毕业模板等后处理后，只能由
`tools/game_data/promote_static_release.py` 读取仓库外本机配置，原子核对来源哈希、schema/importer、外键、
完整性、数据库与 manifest 后晋升；禁止直接覆盖 `data/` 或用解包 Content 全量覆盖。改变规范化输出的 importer
必须递增 importer 版本。弧盘无条件常驻属性由正式静态关系自动投影；条件效果继续走战斗状态链路。

库存快照不可变，当前指针只指向完整稳定快照。下游在开始时解析一次 `snapshot_id`，运行中不追随最新值；
方案始终按自己的 `source_snapshot_id` 读取。稳定器以完整内容指纹和安静窗口判稳，不保留历史最大数量。
运行时增量只能覆盖原生完整快照中已知 UID 的状态，残缺事件不得新增物品、替换集合或推进指针。

来源能力通过公开 helper 判断：`nte_core` 可提供正式 UID、角色实例与可靠装备状态；视觉来源可分析和普通鼠标
装配，但不能进入极速装配或伪造可靠角色归属。

## 6. 计算、角色与配装

计算冻结账号、generation、快照、静态 dataset、角色顺序/同级组、目标槽位、锁和角色配置，产出不可变
`WeightedAllocationPreview`。保存只消费该 preview，保存前复核冻结边界，不读取“最新状态”补齐。候选构造先剔除
锁定真实 UID；套装、主词条、品质和黑名单使用统一 contract；前序角色已分配 UID 不进入后续候选。虚拟占位评分为
0，不可锁定或极速装配。角色顺序拖拽与同级 `=`/优先 `>` 的语义由公共优先级组测试定义，不得在 UI 重写。

异环工坊权重的装备公式唯一来源是 `ScoringEngine.calculate_drive_score` 与
`ScoringEngine.calculate_cartridge_score`。计算、游戏配装、卡片展示、倒带和官方角色基础评分只能经此引擎或其
无公式适配层计算，不得复制公式。已保存方案的完整 `assignment_scores` 是单件卡片、等级、累计分与替换差值的冻结
事实；旧方案缺失该字段时才可按同一引擎重算。替换弹窗可用当前角色直伤边际权重排序和展示，但保存方案必须写入
同一工坊评分契约下的冻结单件分数。

官方角色默认取静态毕业模板，账号显式配置优先。有效角色面板统一消费等级/突破、弧盘等级/突破、无条件常驻属性、
好感度 10、家具加成、具体觉醒与技能等级。基础暴击率为 5%、基础暴击伤害为 54%；角色暴击上限扣除专武、弧盘
无条件暴击和好感度暴击，手工上限只能收紧；全局优先不消费角色管理中的暴击最小/上限。条件弧盘效果不进入常驻
面板、毕业率或分配评分。

`slot_id` 是同角色多方案的稳定身份，`slot_name` 仅展示。当前槽位方案按自己的来源快照保存；跨角色当前槽位
引用同一真实 UID 才冲突。锁是账号内方案锁，不是游戏锁 RPC；锁定方案不得删除、覆盖、归档或借出 UID。
`EquipmentPresentation` 是装备卡片、评分、收益和差异的唯一公共展示组件。

## 7. 扫描、仓库、装配与倒带

仓库读取固定快照，状态操作先生成计划，再由 Integration 执行，后续完整快照或正式范围事件确认。视觉扫描只有
完整结果一次提交；取消、异常或数量不一致不提交半成品。扫描、鉴定、倒带与自动装配都接入应用级停止键，停止
路径必须释放输入状态。

极速装配只消费原生正式 UID、角色实例和已保存槽位；游戏界面自动装配可消费视觉投影。两者冻结上下文和 token，
执行中阻止账号切换。倒带推荐只读固定库存、所选角色的当前已保存方案及其冻结单件评分；显式“生成推荐”才按
评分缺口和用户选择的均衡/少角冲分策略求解；执行前冻结品质与八槽方案。

## 8. 战报

战报采集只保存 Core 的摘要、record、axis 与原始逐击；不得从聚合摘要猜暴击、Buff/Debuff、护盾、治疗、
完整队伍、敌人实例或场景。结束后从当时最新完整原生背包和角色有效养成一次性物化副本，不读取活动配装。
没有完整原生背包时，以发行毕业模板冻结假定空幕/驱动和计算面板，标明“毕业模板假定”；角色养成仍优先
使用账号显式配置。假定装备仅供战报计算，不伪造原生背包或正式 UID，后续同步不会替换已冻结的假定配装。
原始角色/装备快照不可修改；战报修改副本是单场账号私有副本，装备覆盖复制完整计算装备，不保留活动指针。
副本只参与固定轴逐击伤害重放和反事实边际计算，绝不改写战报主页面的实测伤害、DPS、时间轴或原始事实。

环境推断、用户确认环境、敌方画像、Buff 审计和边际计算都是版本化派生结果，必须与 Core 原始事实和置信度分离。
确认环境保存面向玩家的中文环境名；原始类路径只可作为内部匹配字段或诊断，不得用于主界面标签。未知不得伪装为
零、倍率一或完整收益。采集 stop 超过 12 秒必须中止本次 Core、丢弃未完成暂存并进入 error；空战报不得无限等待
最终 axis。战报仍在迭代时，未通过的战报专项回归必须单列，不得改写为通用通过。

## 9. 外部集成、UI 与日志

nte-core、Npcap、mods、OCR、鼠标/手柄、二进制和游戏输入属于 Integration。第三方组件晋升前记录上游版本、
提交、许可和 SHA-256，并完成协议、打包与真实 Windows 验证；根目录本机二进制不入库。

新弹窗使用 `src.app.window_geometry`，按当前屏幕可用区域限制并相对 owner/屏幕居中，覆盖混合 DPI；自定义颜色、
选中态和控件状态必须同时兼容原色、黑色和白色主题。日志按层记录生命周期和诊断，不记录完整 RPC、UID 列表、
账号显示名、绝对路径、OCR 全文、截图、CDK、Token 或可复原 payload。

## 10. 代码、测试与发布门禁

- `src/`、`tools/`、`tests/` 的新增或修改 Python 文件不得超过 800 行；触及存量超限文件时只可拆分或收缩。
- 新增 Python 文件首行必须是中文摘要注释；新 `type: ignore` 必须带错误码与原因。
- Ruff `E9/F63/F7/F821/F401` 是硬门禁；新增依赖同步更新 `pyproject.toml` 与 `uv.lock`。
- 禁止新增 `setattr(MainWindow, ...)`、`globals()` 动态导出、服务定位器、跨 Feature 私有调用和页面索引跳转。
- 不提交账号库、WAL/SHM、日志、截图、PCAP、OCR 临时文件、构建/安装器产物、本机 SDK/转储或未审计二进制。

发布前至少完成：静态检查、`core`、`full`、打包输入核对、升级/回滚路径、干净安装与主题默认值验证、账号切换、
背包同步、关键计算/保存/装配的实机烟测。已知失败必须有明确归属与用户可见边界；未验证的功能不得标为稳定。
