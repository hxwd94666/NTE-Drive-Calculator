# NTE Drive Calc：开发 Agent 契约

本文件供进入本仓库的开发 Agent 先读。它只记录跨功能的产品事实、数据安全不变量、架构边界与完成门禁；页面细节、公式表、外部协议和待办各有单独事实源。执行任务前先核实当前分支、工作区和实际代码，不从上次对话或发行版本推断现状。本文件应可公开：账号、凭据、真实 UID、抓包、截图、OCR 原文、未脱敏日志、私有地址及本机绝对路径均不得写入。

## 1. 事实源与文档所有权

发生冲突时，先看已通过的公共行为测试、数据库约束和正式协议，再看本契约；随后看领域文档，最后看当前 UI 表象。版本、schema、能力名和 dataset 身份以代码、清单及数据库为数值事实源，本文件不抄写易过期数字。现有代码存在遗留跨层调用，不得把“代码里有”误认成新实现可以继续采用的架构模式。

| 问题 | 唯一应查位置 |
| --- | --- |
| 系统分层、组合根、所有权、生命周期 | `docs/architecture.md` |
| 已交付的用户行为 | `docs/features.md` 及 `docs/features/` |
| 进程、协议、静态构建、发行组件 | `docs/integrations.md` |
| 工作模式与能力授权 | `docs/reference/work-modes.md` |
| 公式、静态字段、养成、战报格式和机制 | `docs/reference/`，索引见 `docs/README.md` |
| 未交付或仍待证实的能力 | `docs/roadmap.md` 及 `docs/roadmap/` |
| 真实 Windows 验收 | `docs/validation/windows.md` |

修改文档时覆盖式重写责任章节：删除失效结论、重复说明和阶段流水账；一个事实只留一处权威表述，其余位置链接引用。已交付行为进入 features，未完成事项进入 roadmap，硬边界留在本文件。仓库不维护独立发行说明；发行范围由版本、提交、构建输入和当次发布平台信息共同确认。

## 2. 产品能力地图与不可跨越的语义边界

| 领域 | 产品职责 | 必守边界 |
| --- | --- | --- |
| 账号与工作台 | 账号切换、稳定快照摘要、同步状态、快捷入口 | 账号数据隔离；切换前停止旧任务并推进账号代次 |
| 工作模式 | 离线、低风险、中风险、开发四种本机模式 | 下拉框仅为草稿，用户确认才生效；降级先撤销副作用权限 |
| 游戏数据同步 | 抓包或原生背包与角色状态采集 | 自动同步只启动同步，不连带启动战报、装配、弃置、锁定或插件 |
| 计算与分配 | 固定库存上的角色优先分配、预览、保存 | 全部输入冻结；保存仅消费该预览，不用“最新状态”补齐 |
| 角色 | 等级、突破、技能、好感、家具、觉醒、弧盘和权重 | 原生同步只覆盖已证实字段；缺失和未知保留原值 |
| 配装 | 游戏配装、计算配装、多槽位、锁、替换、属性汇总 | `slot_id` 是身份；每份方案保留自己的来源快照 |
| 仓库与鉴定 | 固定库存浏览、状态管理、截图/剪贴板/手工鉴定 | 视觉库存不读取或修改游戏锁/弃置；鉴定不写背包与配装 |
| 扫描与输入 | 全量扫描、游戏界面装配、倒带等 | 完整结果一次提交；取消、数量异常或输入失败不留半成品 |
| 极速装配 | 原生 UID、角色实例和已保存槽位的批量装配 | 指令接受不是成功；新后验状态确认前结果未知时不自动重发 |
| 战报 | 原始采集、冻结事实、历史和独立分析 | 原始事实不可改；派生结果带版本与来源；未知不伪装为零 |
| 插件 | 技能冷却、敌人状态 HUD | 两张卡独立维护状态；关闭插件不启停战报 |
| 工具与资料库 | 倒带、养成计算器、只读静态资料库 | 冻结 dataset；养成计算器不写角色档案、背包或活力推测 |
| 环境与更新 | 路径、组件、运行库、部署、清理、镜像更新 | 路径、文件、进程、握手、能力和业务就绪必须分开判断 |

工作模式具体能力集合、提示及自动同步边界以 `docs/reference/work-modes.md` 为准。高风险动作须同时满足已确认的模式、未安全暂停、组件身份与协议有效、相应业务能力就绪、本次输入证据完整。文件存在、进程存活、连接成功、握手成功、能力声明和业务可用是六个状态。

## 3. 仓库职责、分层和身份

本仓库是 Calc 产品与集成端，拥有账号数据、界面、配置、计算编排、进程生命周期、协议适配和结果展示。公共采集修复在 toolkit 公共 fork；私有分析及专用组件在独立集成仓库。本仓库只消费有版本、来源、清单和哈希的组件包，不复制私有源码或以旁路替代正式协议。`nte-core.exe` 负责采集与游戏交互，`nte-analysis-core.exe` 负责战报分析；二者身份、能力、部署和验证独立。

平台基线为 Windows 10/11、Python 3.11、PySide6。`main.py` 是入口，`src/ui/app.py` 是组合根。依赖以 `pyproject.toml` 和 `uv.lock` 为准，应用版本以 `src/app/version.py` 为准。目标依赖方向：

```text
UI Page/View → Controller → Application Service → Domain/Optimizer 或 DAO/Integration
```

- UI 只拥有控件和可丢弃的展示状态；Controller 拥有 worker、取消、页面投影与窄导航，不实现公式、SQL 或数据兼容。
- Service 冻结请求并编排跨 DAO/Integration 的事务；Domain/Optimizer 执行纯计算，不读取 Qt 控件、文件、进程或数据库。
- DAO 独占 schema、SQL、迁移、事务和一致性备份；Integration 独占进程、文件、协议、OCR、输入和系统探测。
- `AppContext`、`EquipmentPresentation`、全局热键和共享原生会话由组合根创建并显式注入。新代码不引入服务定位器、动态挂载 MainWindow、跨 Feature 私有调用或长期新旧双入口。对遗留违反分层处，按任务边界收敛，不扩大依赖。
- 跨层关系以正式 `character_id`、`item_id`、`suit_id`、`shape_id`、`property_id`、装备 UID 和 `slot_id` 连接；中文名、昵称、`slot_name` 只展示。导航以 key/`parent_key` 定位，不靠页号或遍历控件。

主线与外部组件分支都须以实际 Git ref 和清单重新核实。源码合入、构建成功、组件晋升、打包成功和真实游戏验收互不替代。

## 4. 数据域、冻结身份与账号生命周期

| 数据域 | 所有权与读取规则 |
| --- | --- |
| `data/game_static.sqlite3` | 发行主静态：成长、技能、公式、敌人、离线权重与毕业模板；运行时只读 |
| `data/role_catalog/` | 独立角色/图鉴目录；用途身份为 `role_page` 或 `reference`，带数据库、图片及清单，不自动替代战报 dataset |
| `config/` | 本机主题、工作模式、插件及环境偏好；不得随账号导入导出 |
| `accounts/<account_id>/user_data.sqlite3` | 当前账号私有快照、角色、权重、槽位、方案、锁、任务及战报；只由当前账号上下文访问 |
| `data/app_shared.sqlite3` | 仅 contract 明示允许的旧迁移共享数据；不覆盖官方静态或账号显式配置 |
| `build/` 与临时目录 | 可重建运行/构建产物，不作当前事实源，也不随账号或发行包外发 |

读取优先级为账号显式配置 → contract 明许的共享数据 → 发行默认；主题、模式和插件的本机归属遵循各自文档。`AppContext` 是路径和账号状态的唯一组合根，`generation` 是账号代次。切换顺序为阻止新操作、停止旧账号 owner、替换上下文并递增代次、重建窄服务、清空页面缓存、恢复被明确允许的自动服务。

每个长任务创建时冻结账号、generation、路径、静态 dataset、`snapshot_id`、配置版本、目标 `slot_id`、锁快照和取消 token；回调、预览保存、数据库写入及外部指令前复核。过期结果静默丢弃，不写新账号、不刷新新页面。取消只使本次 token 失效；owner 以公开 `close/stop` 收尾。账号切换、模式降级和退出先撤销新副作用权限，再有界等待原任务结束。

SQLite schema 与静态 importer 采用追加迁移；已发布迁移文件不得改义或重排。schema、payload、协议或错误码变化覆盖新建、旧版升级、失败回滚和修复后重试，不以 UI 双读长期兼容。多表业务写入用单一事务；JSON 配置写同目录临时文件并原子替换；SQLite 活库备份用一致性备份，不能复制活动 WAL/SHM 作为备份。

账号导出只包含账号运行数据、必要配置及 contract 允许的基线资源，不携带日志、缓存、抓包、凭据和其他账号数据。导入先验证格式、成员路径、重复成员、总量和用户库，再在账号目录内暂存、迁移并原子切换；失败保留原账号、索引和当前会话。存量流程若缺任一门禁，应补齐门禁，而不是把现有行为当作安全证明。删除、覆盖、归档和清理按正式身份及所有权执行，不用显示名或单一扩展名扩大范围。

## 5. 来源能力、库存完整性与静态数据

主静态只由 `tools/game_data/` 在 `build/` 构建候选，经过规范化、必要后处理和校验，再通过 `tools/game_data/promote_static_release.py` 按本机配置核对来源哈希、schema/importer、外键、完整性、大小和清单后原子晋升。不得直接覆盖 `data/`、不得把解包 Content 全量当规范化结果；改变规范化语义时递增 importer 版本。

同版本战斗来源不足时可独立构建 `role_page` 或 `reference` 图鉴，按明确优先级逐文件记录来源与哈希。`reference` 可作为装备评分和分配的一次完整静态输入；同一次计算只用一个校验过的目录，不与旧主库混读。图鉴不赋予新的伤害公式、毕业模板、战报或原生同步能力。资料目录只经正式晋升入口成套安装；数据更新后旧预览需重新发起。

库存当前指针仅指向完整且稳定的不可变快照。下游任务开始时只解析一次 `snapshot_id`；方案始终读取自己的 `source_snapshot_id`。稳定器核对完整性、正式身份、数量一致性、UID/角色关系和内容指纹，再等安静窗口；不按历史最大件数猜完整。运行时增量只覆盖正式完整快照中的已知 UID 状态，残缺事件不新增集合、不推进当前指针。

来源能力由公开 helper 判定，并将覆盖范围及未知值传到消费端：

- `nte_core` 仅在能力与完整性证据成立时提供正式 UID、角色实例、装备关系、锁/弃置状态和原生写入。
- 抓包可生成完整库存和战报事实，但不伪造原生角色实例、装备写入或 DLL 业务就绪。
- 视觉来源可分析、鉴定和执行普通鼠标装配；状态未知时禁用锁/弃置，不进入极速装配，也不伪造可靠角色归属。
- 历史快照、战报冻结子集、毕业模板假定和用户编辑副本均非当前完整库存，不提升为正式背包或生成真实 UID。

`unknown`、`partial`、`complete=false`、缺页、修订变化和字段映射冲突均不提交正式集合；不拿旧快照、固定件数、抓包结果或默认零静默补齐原生缺项。

## 6. 计算、角色、评分与配装

计算冻结账号、generation、库存快照、静态 dataset、角色顺序/同级组、有效面板、目标槽位、锁、配置和组合上限，产生不可变 `WeightedAllocationPreview`。保存只消费该预览，并在提交前复核冻结边界。候选先排除锁定真实 UID；套装、主词条、品质、词条优先和黑名单走统一 contract。同一真实 UID 在全部角色与槽位只能消费一次；虚拟占位得分为零，不参与锁定、借出或极速装配。取消贯穿枚举、预留、恢复和提交，不覆盖旧方案。

分配策略唯一为角色优先。同级组按实际得分联合分配；高优先角色严格同分候选以槽位为最小单位延迟归属。Top-K 仅识别预留回填冲突；恢复仍做一对一匹配并复核形状、套装、锁、UID/卡带唯一性、暴击上下限及词条约束。单角色/单组失败只令该结果无效，不中断后续角色。

装备基础评分唯一公式入口为 `ScoringEngine.calculate_drive_score` 和 `ScoringEngine.calculate_cartridge_score`。计算、游戏/计算配装、卡片、倒带和官方角色基础评分调用同一引擎或无公式适配层，不复制公式。保存方案的 `assignment_scores` 是逐件分数、累计分与替换差值的冻结事实；旧方案缺失时才按同引擎重算。替换优化可按当前角色直伤边际动态权重排序，其余场景使用基础权重；缺正式模型的自建角色同样使用基础权重。

官方角色默认读取发行静态毕业模板，账号显式配置优先。有效角色面板统一消费等级/突破、弧盘等级/突破、无条件常驻属性、好感十级、家具、具体觉醒和技能基础等级。基础暴击率、暴击伤害、角色暴击上限及扣除项只在角色面板公共服务计算；计算、毕业率和配装汇总消费同一投影。条件弧盘效果不进入常驻面板或基础分配评分。

`slot_id` 为同角色多方案稳定身份，`slot_name` 只展示。每份方案保留自身来源快照和冻结评分；跨角色当前槽位引用同一真实 UID 才冲突。Calc 方案锁不是游戏锁 RPC；锁定方案不得删除、覆盖、归档或借出 UID。`EquipmentPresentation` 是装备卡片、词条颜色、评分、收益与差异的公共展示入口。

## 7. 工具、同步、扫描和外部写操作

首页自动同步偏好与模式授权独立：关闭监听不停止手动战报、手动角色同步或已授权的组件管理。低风险使用抓包，中风险使用原生 DLL；开发模式的正式库存仍由 DLL 写入，抓包用于战报对照。重新同步保留已保存背包及方案，只重建未提交候选。未登录、等待游戏及初始化是等待，协议或完整性错误是具体故障。

角色同步复用已运行的游戏数据会话，不新建第二连接。仅提交本次返回且被支持的永久字段；未返回角色、未知字段、试用/临时角色和关联冲突保留旧配置。明确未装备弧盘才清除旧弧盘；技能以正式 SkillID 匹配，不把觉醒加级重复写进基础等级。字段组格式或关联校验失败时跳过该组，其余确认字段在单一事务保存；读取未完成、取消、代次失效或写入失败时本次更新整体不提交，也不改历史战报。

养成计算器读取冻结静态资料与用户当前输入，单/多角色目标和已有材料草稿不回写角色档案或背包。结果区分材料总量、已有扣除、合成换算和可由副本推定的体力；仅体力视图排除主要非副本来源的方斯。三份低阶换一份高阶只在已确认的同族材料链使用，角色与弧盘经验材料不参与换算。刷本产出、鉴别等级及活力推算按 `docs/reference/progression-stamina-calculator.md` 的可观测范围表达，缺少产出证据时保留未知。

仓库锁定/弃置、极速装配、游戏界面装配、倒带和其他外部写操作遵循“计划 → 冻结 → 派发 → 后验确认”：计划写明对象、期望状态、来源和幂等边界；派发前复核账号代次、快照、能力、游戏上下文及取消；明确未派发的忙碌才有限重试；超时或未知结果停止后续，不重发可能已经执行的动作。成功由新完整快照或正式范围事件确认，RPC 接受、按钮状态及进程存活都不等于业务成功。失败保留旧持久事实，释放临时文件、输入状态、租约和 worker。

仓库读取固定快照；“弃置”设置游戏标记，不删除装备。视觉扫描、鉴定、倒带及界面装配接入应用级停止键并释放鼠标、键盘、手柄和窗口状态。极速装配只消费原生正式 UID、角色实例和已保存槽位；视觉装配使用视觉投影，两者冻结账号、来源和 token，执行期阻止账号切换。倒带读取固定库存、所选角色当前活动方案和冻结逐件评分，仅显式“生成推荐”才求解。

## 8. 战报：原始事实、派生结果与独立分析

采集层只保存 Core 给出的摘要、record、axis、原始逐击和明确观测；不从聚合摘要猜暴击、Buff/Debuff、护盾、治疗、完整队伍、敌人实例或场景。战报开始冻结账号代次、静态 dataset、账号允许的回退输入及来源能力。抓包沿用开始时配装；原生采集在每半场第一击独立冻结队伍、角色、装备和环境证据。冻结后不追随后续背包或配置变化。

战报结束只保存该次尝试所需角色面板及已装备物品，不附整背包，也不依赖背包快照继续存在。编辑和边际候选在打开页面时从当前稳定背包一次冻结。缺正式原生背包时，发行毕业模板仅可作明确标注的战报计算假定，不进入正式库存、不产生 UID。

原始角色/装备快照不可修改。编辑副本为账号私有、单场、单一活动副本，只参与固定轴重放和反事实分析，不改实测伤害、DPS、时间轴或原始事实。环境推断、用户确认环境、目标画像、Buff 审计、治疗和边际结果均为带算法版本、静态身份、置信度及来源的派生数据；未知不转成零、倍率一或“完整”。导入导出格式见 `docs/reference/battle-report-package.md`。

战报页面分析只经独立 `nte-analysis-core.exe` 的数据库只读接口。缺清单、哈希、协议、引擎版本或所需页面能力时不回退到 Python 直接读库。请求冻结账号、战报 ID、数据库/配置路径、静态 dataset、分析范围与用户候选；缓存仅属当前账号会话，身份变化即失效，不写原始事实。采集停止超时终止本次 Core 并丢弃未完成暂存；空战报不无限等待 axis。

## 9. 组件、部署、静态晋升与发行瘦身

Npcap、Core、独立分析组件、原生 DLL/Loader、OCR、鼠标/手柄和游戏输入均属 Integration。组件晋升记录上游版本、提交、许可和 SHA-256；DLL、Loader、脚本、资源和协议作为一套输入，不单件替换。私有交付只含核对过的执行物、公开脚本/资源、组件清单和许可通知；源码归档、PDB、账号数据、凭据、抓包和敏感日志不进仓库、安装包或附件。

游戏路径必须是校验过的 `HTGame.exe` 绝对路径；自动发现只有唯一有效候选时才保存。自动部署只管理当前发行包内已核对组件及本机已选加载方式，不提升工作模式权限。部署/升级以正式文件名、清单、记录和确切哈希判定归属；未知同名文件保留并报告。程序明确退出或执行已记录的安全清理时，依正式清理契约可按归属目录与确切程序文件名收尾；不能把升级的旧哈希接管规则误套到所有清理路径。游戏运行、路径未确认、权限不足或占用时进入明确等待/故障，不强关游戏。旧部署路径失效仍按原记录绝对目录处理；Loader 独立收尾。注册表及常见目录只作有限发现，不递归扫盘或删除未验证目录。细则见 `docs/reference/game-component-bundle.md`。

分析组件须以执行文件、`component.json` 和许可证成套部署，清单哈希、引擎版本及页面能力共同构成兼容性。Mirror 更新链分别处理版本检查、下载、取消和安装器启动；失败保留可运行旧版本与可重试状态，不记录鉴权 URL、Token 或 CDK。

静态库体积优化只在隔离候选中做重打包，逐表验证逻辑等价、schema/索引、外键、来源与清单后再晋升；不是运行时 `VACUUM` 账号库或直接改发行数据。安装器资源去重仅针对字节完全相同的图鉴图片，并还原应用期待的两个原始路径。运行库裁剪使用经审计的精确清单与打包烟测，不以模糊目录删除。打包输入、分析组件能力、三主题和真实 Windows 回归均需在发布前验证；瘦身数值不得替代功能验收。

## 10. UI、日志和可观察性

新弹窗使用 `src.app.window_geometry` 按当前屏幕可用区域限位并相对 owner/屏幕居中，覆盖混合 DPI。重要提示按“状态 → 原因 → 下一步”组织；警告明确层级、默认取消并使用系统提示音。自定义颜色、选中/禁用/焦点状态及控件尺寸同时适配原色、黑色和白色主题。状态标签归操作 owner，无关功能不共享“正在应用”之类瞬时状态。

用户可见文本区分等待、不可用、故障、已保存、已部署、已确认；未检测不写缺失，RPC 接受不写完成。帮助文案只留决策所需信息，详细诊断进检测详情或结构化日志。日志只记生命周期、冻结身份、状态迁移、错误码和耗时，按 `docs/reference/logging-events.md` 脱敏；不记账号显示名、完整 UID 列表、绝对路径、完整 RPC、可复原 payload、OCR 原文、截图、CDK、Token、鉴权 URL、原始逐击或完整伤害表。常驻日志归当前账号；账号切换结束旧会话，详细日志使用独立时间戳文件。日志、截图与抓包不是账号事实源，也不进默认导出或发行物。

## 11. 开发顺序、测试与发布完成定义

开始前明确输入/输出数据域、来源能力、冻结身份、外部副作用、失败回滚和验证证据。产品语义变更按“公共行为测试 → contract/文档 → Domain/Service → DAO/Integration → Controller/UI → 迁移全部调用方并删除旧入口 → 专项验证 → core/full 与静态检查”推进；缺陷先复现，修最小业务原因。纯文档任务不借机改生产代码。修改任何文件先看 `git status`，保留其他开发者的未提交状态。

- `src/`、`tools/`、`tests/` 新增或修改 Python 文件不超过 800 行；触及超限存量文件只拆分或收缩。新 Python 文件首行是中文摘要注释；新 `type: ignore` 带错误码和原因。
- Ruff `E9/F63/F7/F821/F401` 是硬门禁；新依赖同步改 `pyproject.toml` 与 `uv.lock`。质量入口为 `tools/quality/run_tests.py`，`core` 覆盖关键边界，`full` 完整发现。
- 行为测试覆盖计算、持久化、迁移、账号隔离、来源/完整性、并发取消、后验确认、权限、可见数据语义、键盘可达性和窗口不越界。长期回归不固化颜色、像素、装饰图、边距或私有控件结构；响应式布局只在控件可操作性与内容不被遮挡方面属于行为 contract。过时测试改写或删除，不为废弃入口造兼容层。
- 默认不编译、不运行测试；用户明确授权后只执行授权范围。未运行项目如实说明。静态检查、专项测试、`core`、`full`、打包和真实 Windows 验收是不同证据；失败分清本次新增、既有失败、可选组件缺失和环境不足，不靠删测或跳过掩盖生产缺陷。
- 不提交账号库、WAL/SHM、日志、截图、PCAP、OCR 临时文件、构建/安装器产物、本机 SDK 或转储。提交、推送、发布、安装器构建、外部组件部署及真实游戏操作均按用户明确授权执行。

发布准备入口为 `tools/release/prepare_release.py`，只输出本机核对与维护者手工命令，不自行推送或上传。正式发布前核对统一版本、dataset、组件清单、升级和回滚、干净安装、三主题、账号切换、背包/角色同步、关键计算/保存/装配、组件部署清理及真实 Windows 烟测。发布构建应拒绝缺失、不匹配或无所需页面能力的分析组件；未完成验证的能力不得标为稳定。
