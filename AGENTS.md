# NTE Drive Calc 仓库开发契约

本文件只定义当前产品的核心能力、数据安全不变量、架构边界与工程门禁。页面行为见
`docs/features.md`，系统结构见 `docs/architecture.md`，外部组件见 `docs/integrations.md`，工作模式见
`docs/reference/work-modes.md`，公式和格式见 `docs/reference/`，未完成事项见 `docs/roadmap.md`，真实 Windows
验收见 `docs/validation/windows.md`。本文件必须可公开，不记录账号、Token、CDK、私有地址、真实 UID、用户绝对路径、
完整抓包、OCR 全文、截图、未脱敏日志或私有仓库信息。

## 1. 事实源与文档维护

冲突时依次采用：已通过的公共行为测试与数据库约束、本文件、架构/功能/集成文档、公开协议、当前 UI。
代码中的版本与 schema 常量是数值事实源；本文只约束读取、迁移和兼容原则，不复制容易过期的数字。

文档按职责维护：

- `AGENTS.md`：跨功能硬契约、数据安全、仓库和组件边界。
- `docs/architecture.md`：分层、组合根、数据流、所有权与生命周期。
- `docs/features.md`：已经交付且可由用户观察的行为。
- `docs/integrations.md`：外部进程、协议、插件、输入、OCR、静态构建和组件包。
- `docs/reference/`：公式、字段、格式和窄领域 contract。
- `docs/roadmap.md`：尚未完成、尚未稳定或仍待外部事实的能力。
- `docs/validation/`：真实环境验收步骤和证据要求。

更新文档必须覆盖式重写相关章节：删除失效结论、重复说明、阶段流水账和已经完成的计划；一个事实只保留一个
权威位置，其余位置链接引用。不得在文末堆叠“补充”“变更记录”或临时调查结论。仓库不维护
独立发行说明文件；发布范围由版本、提交历史、构建输入和发布平台当次信息共同确认，不在仓库复制一份易失真的说明。

## 2. 仓库职责与外部组件

本仓库是 Calc 产品与集成端，负责账号数据、界面、配置、计算编排、进程生命周期、协议适配和结果展示。
公共采集能力及通用上游修复由 toolkit 公共 fork 维护；私有分析和专用组件由独立集成仓库维护。本仓库不接收
私有源码，也不以复制外部实现的方式绕过正式协议。

- `nte-core.exe` 是采集与游戏交互进程；`nte-analysis-core.exe` 是战报分析进程。二者独立构建、版本化、部署和管理，
  不得混用进程身份、能力声明或源码。
- Calc 只采用明确版本、明确来源和完整哈希的组件包，不自动追随外部最新分支。DLL、Loader、脚本、资源和协议
  视为一套输入；只替换其中单件不构成完整升级。
- 私有交付物只包含已核对的可执行程序、公开脚本与资源、组件清单和许可通知。源码归档、调试符号、账号数据、
  凭据、抓包与敏感日志不进入公开仓库、安装包或发布附件。
- 公共缺陷回公共 fork，私有分析缺陷回对应组件仓库。跨仓库同步只携带任务所需的公开 contract、构建输入和产物，
  不复制对方仓库的主线、凭据或专属开发说明。
- Calc 主线为 `main`，外部仓库分支约定只作提示。任何合并、构建、部署前都重新核实实际仓库、ref、远程和工作区。
  源码合入、构建成功、组件晋升、打包成功和真实游戏验收是不同状态，不得互相替代。

## 3. 核心产品能力与用户边界

| 能力域 | 当前职责 | 硬边界 |
| --- | --- | --- |
| 账号与工作台 | 账号切换、稳定快照摘要、同步状态、快捷入口 | 账号数据互相隔离；切换账号先停止旧任务并递增 generation |
| 工作模式 | 离线、低风险、中风险、开发四种本机授权 | 下拉选择只是草稿；用户确认后才生效，取消必须回退到原模式 |
| 游戏数据同步 | 低风险抓包；中风险/开发原生背包与角色状态 | 自动同步只启动同步，不启动战报、装配、弃置、锁定或插件 |
| 计算 | 固定快照上的角色优先分配、预览、保存 | 计算冻结全部输入；保存只消费该预览，不用“最新状态”补齐 |
| 角色 | 等级、突破、技能、好感、家具、觉醒、弧盘与权重 | 原生同步仅覆盖已证实字段；未知和缺失字段保留原值 |
| 配装 | 游戏配装、计算配装、多槽位、锁、替换与属性汇总 | `slot_id` 是身份；方案引用自己的来源快照；游戏导入与计算方案语义分离 |
| 仓库与鉴定 | 固定库存浏览、状态管理、截图/剪贴板/手工鉴定 | 视觉库存不读取或修改游戏锁/弃置；鉴定不写背包和配装 |
| 扫描与输入 | 鼠标/手柄全量扫描、装配、倒带等界面操作 | 完整结果一次提交；取消、数量不一致或异常不保存半成品 |
| 极速装配 | 原生 UID、角色实例和已保存槽位的批量装配 | 指令接受不等于成功，必须由正式后验状态确认；未知结果不自动重发 |
| 战报 | 原始采集、冻结事实、历史记录和独立分析组件计算 | 原始事实不可修改；派生结果版本化；未知不伪装为零或完整收益 |
| 插件 | 技能冷却、敌人血量与倾陷 HUD | 两张卡独立维护状态；关闭插件不启停战报；仅中风险/开发可启用 |
| 工具与资料库 | 倒带、养成计算器、只读静态资料库 | 固定 dataset 读取；养成计算器不写角色档案、背包或活力推测 |
| 环境与更新 | 路径、Npcap、组件、运行库、部署、清理与检测 | 找到路径不等于组件可用；文件、握手、能力、业务就绪逐项判断 |

工作模式的能力集合、确认文案、自动同步和组件生命周期以 `docs/reference/work-modes.md` 为唯一详细事实源。
高风险能力必须同时满足：模式已确认、未处于安全暂停、组件身份有效、协议兼容、具体业务能力就绪以及本次数据证据完整。
文件存在、进程存活、管道可连接、握手成功、能力声明和业务可用是六个不同状态。

## 4. 技术基线与分层

- 平台为 Windows 10/11、Python 3.11、PySide6；入口为 `main.py`，组合根为 `src/ui/app.py`。
- 依赖唯一来源为 `pyproject.toml` 与 `uv.lock`；应用版本唯一来源为 `src/app/version.py::__version__`。
- 质量入口为 `tools/quality/run_tests.py`；`core` 覆盖关键边界，`full` 执行完整发现。

```text
UI Page/View → Controller → Application Service → Domain/Optimizer 或 DAO/Integration
```

- UI 只拥有控件和可丢弃展示状态，不直接操作数据库、外部进程或业务事务。
- Controller 只拥有 worker、取消、页面投影和窄导航，不实现公式或数据兼容。
- Service 冻结请求、编排事务和跨 DAO/Integration 流程，不查找 MainWindow 或 Feature 私有对象。
- Domain/Optimizer 只做不可变纯计算，不读取文件、Qt 控件、进程或数据库。
- DAO 独占 schema、SQL、迁移、事务和一致性备份；Integration 独占进程、文件、协议、OCR、输入和系统探测。
- `AppContext`、`EquipmentPresentation`、全局热键和共享原生会话只在组合根创建并显式注入。

跨层关系统一使用正式 `character_id`、`item_id`、`suit_id`、`shape_id`、`property_id`、装备 UID 和 `slot_id`。
中文名、昵称和 `slot_name` 只用于展示。导航使用 key/`parent_key`，不以页号、控件遍历或 MainWindow 字段扫描定位功能。
禁止新增服务定位器、`globals()` 动态导出、`setattr(MainWindow, ...)`、跨 Feature 私有调用和新旧入口长期并存。

## 5. 上下文、异步与副作用

`AppContext` 是路径和账号状态的唯一组合根：`ApplicationPaths` 管发行资源与应用路径，`AccountContext` 管当前账号，
`generation` 标识账号代次。账号切换顺序固定为：阻止新操作 → 停止旧账号 owner → 替换上下文并递增 generation →
重建窄服务 → 清空页面缓存 → 恢复明确允许自动运行的服务。

每个长任务在创建时冻结账号、generation、路径、静态 dataset、`snapshot_id`、配置版本、目标 `slot_id`、锁快照和
取消 token；回调、预览保存、数据库写入和外部指令前再次核对。过期结果静默丢弃，不得写入当前账号或刷新当前页面。
取消只使本次 token 失效；owner 通过公开 `close/stop` 收尾，不修改 worker 私有状态。执行中账号切换、模式降级和退出
必须先撤销新副作用权限，再有界等待已有任务收尾。

外部写操作统一采用“计划 → 冻结 → 派发 → 后验确认”：

1. 计划明确对象、预期状态、来源和幂等边界。
2. 派发前复核账号代次、快照、能力、游戏上下文和取消 token。
3. 明确未派发的忙碌只允许有界重试；超时或结果未知停止后续指令，不自动重复可能已执行的动作。
4. 成功由新的完整快照或正式范围事件确认，RPC 接受和按钮状态不作为业务成功证据。
5. 失败保留原持久事实，临时文件、输入状态、租约和 worker 均须释放。

## 6. 数据域、持久化与迁移安全

| 数据域 | 位置 | 所有权与规则 |
| --- | --- | --- |
| 发行静态 | `data/game_static.sqlite3` | 官方目录、成长、技能、公式、敌人、离线权重和毕业模板；运行时只读 |
| 独立资料目录 | `data/role_catalog/` | 带用途身份、数据库和图片清单的角色/图鉴资源；不替换战报或计算默认 dataset |
| 应用全局 | `config/` | 主题、工作模式、插件和本机 UI/环境偏好；不得随账号导入导出 |
| 账号私有 | `accounts/<account_id>/user_data.sqlite3` | 快照、角色、权重、槽位、方案、锁、任务和战报；只由当前账号上下文访问 |
| 公共共享 | `data/app_shared.sqlite3` | 仅保留明确允许的旧迁移数据；不得覆盖官方静态事实或账号显式配置 |
| 运行临时 | `build/`、临时目录、进程内缓存 | 可重建，不作为当前事实源，不得随账号或发行包外发 |

读取优先级为账号显式配置 → contract 明确允许的共享数据 → 发行默认。主题、工作模式、插件等本机配置各自按文档定义
归属，不得因为账号切换而隐式复制。SQLite schema 与静态 importer 只追加迁移；已发布迁移文件不得改义或重排。
新旧 schema、payload、协议或错误码变化必须覆盖新建、旧版升级、失败回滚和修复后重试，不以 UI 双读维持兼容。

所有持久化遵守：

- 多表业务写入使用单一事务；写入失败回滚全部业务变化。
- JSON 配置通过同目录临时文件、刷新和原子替换保存；高权限设置保存失败时不生效。
- SQLite 备份使用数据库一致性备份，不复制活动 WAL/SHM 组合。
- 账号导出只包含账号运行数据、必要配置和 contract 明确允许的基线资源；不包含日志、缓存、抓包、凭据或其他账号数据。
- 账号导入先校验格式版本、成员路径、重复成员、大小和用户数据库，再解压到账号目录内暂存区；同名替换在完整校验与
  迁移成功后原子切换。失败保留原目录、原索引和当前账号，不留下半导入状态。
- 删除、覆盖、归档和清理必须按正式身份和所有权执行；不得根据中文名、目录猜测或单一扩展名扩大范围。

## 7. 静态数据、快照与来源能力

静态库只由 `tools/game_data` 在 `build/` 生成候选。候选完成规范化、毕业模板和必要后处理后，只能通过
`tools/game_data/promote_static_release.py` 读取仓库外本机配置，原子核对来源哈希、schema/importer、外键、完整性、
数据库大小与 manifest 后晋升。不得直接覆盖 `data/`，不得用解包 Content 全量替代规范化输出；改变规范化结果的
importer 必须递增版本。页面和计算在一次操作内冻结 dataset 身份，发现更新后要求重新发起，不混用新旧静态事实。

缺少同版本战斗来源时可构建独立 `role_page` 角色目录或 `reference` 只读图鉴。图鉴构建可按明确的来源优先级
补齐缺项，逐文件记录来源与哈希；运行时各领域只读冻结后的单一数据集，不生成完整战斗能力或毕业模板。
图鉴轨外按赛季配置展示名称与规则，不以限时任务是否存在决定收录，也不自动成为战报的可计算 Buff。
独立目录同样只经正式晋升入口成套安装到 `data/role_catalog/`，不得替换主战报库；详细边界见集成文档。

库存快照不可变，当前指针只指向完整且稳定的快照。下游开始时只解析一次 `snapshot_id`；保存的方案始终读取自己的
`source_snapshot_id`。稳定器以完整内容指纹、来源身份和安静窗口判稳，不以历史最大数量推测完整性。运行时增量只能
覆盖正式完整快照中已知 UID 的状态；残缺事件不得新增物品、替换集合或推进当前指针。

来源能力统一由公开 helper 判断：

- `nte_core` 可在对应能力和完整性证明成立时提供正式 UID、角色实例、装备关系、锁/弃置状态及原生写入。
- 抓包可生成完整库存与战报事实，但不伪造原生角色实例、装备写入能力或 DLL 业务就绪。
- 视觉来源可分析、鉴定和执行普通鼠标装配，但状态未知时禁用锁/弃置，不进入极速装配，也不伪造可靠角色归属。
- 历史快照、战报冻结子集、毕业模板假定和用户编辑副本都不是当前完整库存，不得提升为正式背包或生成真实 UID。

完整性、来源覆盖和未知值必须原样传递。`unknown`、`partial`、`complete=false`、缺页、修订变化或字段映射冲突均不提交
正式集合；不得用旧快照、固定件数、抓包结果或默认零静默补齐原生缺项。

## 8. 计算、角色、评分与配装

计算冻结账号、generation、快照、静态 dataset、角色顺序/同级组、角色有效面板、目标槽位、锁、配置和组合数上限，
产出不可变 `WeightedAllocationPreview`。保存只消费该 preview，保存前复核冻结边界。候选构造先排除锁定真实 UID；
套装、主词条、品质、词条优先和黑名单走统一 contract；同一真实 UID 在全部角色和槽位中只能消费一次。虚拟占位分数
为 0，不可锁定、借出或用于极速装配。取消贯穿枚举、预留、恢复和提交，取消结果不得覆盖已有方案。

分配策略唯一为角色优先。同级组按组内实际得分联合分配；高优先角色的严格同分候选以槽位为最小单位延迟归属。
Top-K 只用于识别预留回填冲突；保护项不得挤掉合法候选，最终恢复仍需一对一匹配并复核形状、套装、锁、UID 唯一性、
卡带唯一性、暴击上下限和词条约束。单角色或单组失败只标记该结果无效，不中断后续角色。

异环工坊基础权重评分唯一来源是 `ScoringEngine.calculate_drive_score` 与
`ScoringEngine.calculate_cartridge_score`。计算结果、游戏配装、计算配装、卡片展示、倒带和官方角色基础评分只能调用
该引擎或无公式适配层，不复制公式。已保存方案的 `assignment_scores` 是逐件分数、累计分和替换差值的冻结事实；
旧方案缺失时才按同一引擎重算。替换优化窗口可按当前角色直伤的边际动态权重排序；除此之外的配装与计算展示使用
角色基础权重。自建角色没有正式角色模型时，替换优化也使用其基础权重。

官方角色默认读取发行静态毕业模板，账号显式配置优先。有效角色面板统一消费等级/突破、弧盘等级/突破、无条件常驻
属性、好感度十级、家具加成、具体觉醒和技能基础等级。基础暴击率、暴击伤害、角色暴击上限及其扣除项只在角色面板
公共服务中计算；计算、毕业率和配装属性汇总消费同一投影。条件弧盘效果不进入常驻面板、毕业率或基础分配评分。

`slot_id` 是同角色多方案的稳定身份，`slot_name` 只展示。每个方案保存自己的来源快照和冻结评分；跨角色当前槽位引用
同一真实 UID 才构成冲突。方案锁属于账号内 Calc 方案，不是游戏锁 RPC；锁定方案不得删除、覆盖、归档或借出 UID。
`EquipmentPresentation` 是装备卡片、词条颜色、评分、收益和差异的唯一公共展示组件。

## 9. 同步、扫描、仓库、装配与插件

自动同步偏好和工作模式授权彼此独立。首页开关只控制背包监听及伴随的后台角色同步；关闭后不停止手动战报、手动角色
同步或已授权的组件管理。低风险使用抓包，中风险使用原生 DLL；开发模式中正式库存仍只由 DLL 写入，抓包用于战报
对照。重新同步保留已保存背包和方案，只丢弃未提交候选并重建会话。未登录、控制超时、等待游戏或组件初始化属于正常
等待；协议错误、完整性错误和正式映射错误保留为具体故障。

角色状态同步只复用已运行的游戏数据同步，不创建第二条连接。仅写入本次返回且已支持的永久字段；未返回角色、未知字段、
试用/临时角色和关联冲突不覆盖账号配置。明确未装备弧盘才清除旧弧盘；技能使用正式 SkillID，不把觉醒加级重复写入
基础等级。字段格式、目录或养成组合校验失败时跳过对应字段组，保留其旧值，其余已确认字段在单一事务中保存；
结果文本列出未采用的角色、字段和正式身份。读取未完成、数据库写入失败、取消或 generation 失效不提交本次更新，
也不修改历史战报。

仓库读取固定快照。锁定/弃置操作先生成计划，再经 Integration 派发，并由新完整快照或正式范围事件确认。“弃置”只设置
游戏标记，不删除装备。扫描模式的区服输入方式属于计算扫描后的游戏操作设置；仓库原生状态管理不展示该设置，也不改写
其已保存值。视觉扫描、鉴定、倒带和游戏界面装配接入应用级停止键；停止路径必须释放鼠标、按键、手柄和窗口状态。

极速装配只消费原生正式 UID、角色实例和已保存槽位。游戏界面自动装配可消费视觉投影。二者都冻结账号、generation、
来源快照、方案和 token，执行期间阻止账号切换。倒带只读取固定库存、所选角色当前活动方案及冻结逐件评分；只有显式
“生成推荐”才求解，执行前再次冻结品质、策略和八槽方案。

插件“技能冷却”和“敌人状态”分别维护偏好、应用状态和运行状态；任一选项变化不得把另一卡片标为“正在应用”。插件
只在已确认的中风险或开发模式启用，降级时关闭启用开关并保留显示偏好。两项都关闭后停止 HUD 发现、属性读取和绘制；
插件不启停战报，也不把显示估计当作战报或计算证据。

## 10. 战报原始事实与派生分析

采集层只保存 Core 给出的摘要、record、axis、原始逐击和明确观测，不从聚合摘要猜测暴击、Buff/Debuff、护盾、治疗、
完整队伍、敌人实例或场景。战报开始时冻结账号 generation、静态 dataset、允许的账号回退输入和来源能力。抓包沿用
开始时固定配装；原生采集在每半场第一击按当前共享基线独立冻结队伍、角色、装备和环境证据。冻结后不追随背包、角色
配置或场景变化。

战报结束只保存保留尝试所需的角色面板和已装备物品，不附带整背包，也不依赖背包快照继续存在。编辑和边际候选在打开
页面时读取当前稳定背包并在本次计算中冻结，不从历史战报拼装候选池。没有完整原生背包时可使用发行毕业模板作为明确
标注的战报计算假定；假定装备不进入正式库存，不生成 UID，也不被后续同步改写。

原始角色/装备快照不可修改。战报编辑副本是账号私有、单场、单一活动副本，只参与固定轴重放和反事实分析，不改写实测
伤害、DPS、时间轴或原始事实。环境推断、用户确认环境、目标画像、Buff 审计、治疗和边际结果都是带算法版本、静态
身份、置信度和来源的派生数据；原始类路径只用于内部匹配，玩家界面使用正式中文名。未知不得转成零、倍率一或“完整”。

战报页面分析只通过独立 `nte-analysis-core.exe` 的数据库只读接口；缺少清单、哈希、协议、引擎版本或
`battle_page_v1` 能力时不回退到 Python 读取数据库。请求冻结账号、战报 ID、数据库/配置路径、静态 dataset、分析范围
和用户候选。缓存只属当前账号会话，输入身份变化即失效，不写回原始事实。采集停止超时须终止本次 Core、丢弃未完成
暂存并进入错误状态；空战报不得无限等待最终 axis。

## 11. 部署、清理、更新与文件所有权

Npcap、nte-core、独立分析组件、原生 DLL、Loader、OCR、鼠标/手柄和游戏输入均属于 Integration。第三方组件晋升前
记录上游版本、提交、许可和 SHA-256，并完成协议、打包和真实 Windows 验证；根目录本机二进制、SDK、PDB 和转储不入库。
分析组件必须以 `nte-analysis-core.exe`、`component.json` 和许可证成套部署，清单哈希、引擎版本与页面能力共同构成
最小兼容契约。

组件部署与清理遵守：

- 游戏路径必须是经校验的 `HTGame.exe` 绝对路径；自动发现只有唯一有效候选时才保存，多候选交给用户选择。
- 自动部署只管理当前发行包中已核对的组件和本机已选加载方式，不提升工作模式权限，不覆盖未知文件。
- 文件归属以部署记录、正式文件名、组件清单和确切哈希共同判断。旧版确切哈希可接管升级；未知同名文件保留并报告。
- 游戏运行、路径未确认、权限不足或文件占用时进入明确等待/故障，不强制关闭游戏，不把待办表述为已确认残留。
- 手动清理先保存安全暂停和清理待办；必要清理不受自动部署暂停影响。清理完成并复核后才清除待办。
- 旧部署路径失效时，清理仍使用原部署记录的绝对目录，不把旧记录套用到新发现目录。Loader 目录独立按所有权收尾。
- 注册表和常见目录只用于有限路径发现；不得递归扫描磁盘，也不得删除未验证目录或扩大到其他游戏。
- 检测详情统一展示文件、进程、连接、握手、能力、业务就绪、快照和完整性；刷新后不得保留操作前的“待清理”快照。

Mirror Controller/Integration 负责版本检查、下载、取消和安装器启动。下载和安装不记录鉴权 URL、Token 或 CDK；更新失败
保留现有可运行版本和可重试状态，不留下被误认为已安装的半成品。

## 12. UI、日志与可观察性

新弹窗使用 `src.app.window_geometry`，按当前屏幕可用区域限制并相对 owner 或屏幕居中，覆盖混合 DPI。重要信息按
“状态 → 原因 → 下一步”展示，普通说明不与按钮混排；警告使用明确层级、默认取消和系统提示音。自定义颜色、选中态、
禁用态、焦点和控件尺寸必须同时兼容原色、黑色和白色主题。状态标签属于具体操作 owner；无关卡片、功能或后台任务
不得共享“正在应用”等瞬时状态。

用户可见文本必须描述真实状态：等待、不可用、故障、已保存、已部署和已确认彼此分离。正常等待不降为错误；失败不写成
笼统“检测失败”；尚未检测不写成缺失；按钮点击和 RPC 接受不写成完成。帮助文本只保留用户决策所需内容，详细诊断进入
检测详情或结构化日志。

日志按层记录生命周期、冻结身份、状态转换、错误码和耗时；不记录账号显示名、完整 UID 列表、绝对路径、完整 RPC、
可复原 payload、OCR 全文、截图、CDK、Token、鉴权 URL、原始逐击或完整伤害表。常驻日志归当前账号，账号切换结束旧
会话并创建新会话；详细日志使用独立时间戳文件。日志、截图和抓包不是账号事实源，也不进入默认导出或发行物。

## 13. 开发流程与完成定义

开始前明确输入/输出数据域、来源能力、冻结身份、外部副作用、失败回滚和验证证据。产品语义变化按顺序完成：

```text
公共行为测试 → contract/文档 → Domain/Service → DAO/Integration
→ Controller/UI → 迁移全部调用方并删除旧入口 → 专项验证 → core/full 与静态检查
```

缺陷先复现，再修复最小业务原因；文档则覆盖式重写受影响章节。新旧语义不得长期双读、双写或由 UI 分流。
完成时必须有行为/生命周期测试、当前文档、迁移后的唯一入口、失败回滚，以及新增失败与既有失败的区分。

默认不编译、不运行测试；用户明确授权后只执行授权范围。未运行项如实说明。按 UTF-8 读取和编辑中文文本；提交、推送、
发布、安装器构建、外部组件部署和真实游戏操作均按用户明确授权范围执行。

## 14. 代码、测试与发布门禁

代码门禁：

- `src/`、`tools/`、`tests/` 的新增或修改 Python 文件不得超过 800 行；触及存量超限文件时只可拆分或收缩。
- 新增 Python 文件首行必须是中文摘要注释；新 `type: ignore` 必须带错误码与原因。
- Ruff `E9/F63/F7/F821/F401` 是硬门禁；新增依赖同步更新 `pyproject.toml` 与 `uv.lock`。
- 静态库、账号库、协议、组件和 UI 版本分别管理；修改应用版本不代替数据库迁移、组件晋升或协议验收。
- 不提交账号库、WAL/SHM、日志、截图、PCAP、OCR 临时文件、构建/安装器产物、本机 SDK/转储或未审计二进制。

测试只固化产品 contract，而不固化装饰实现。必须保留业务计算、持久化、迁移、账号隔离、来源能力、完整性、并发取消、
后验确认、权限门禁、用户可见数据语义、键盘可达性和窗口不越界等行为测试。仅断言颜色值、样式表片段、像素、固定宽高、
边距、控件排列、装饰图片或某个私有控件结构的测试不进入长期回归；同一测试同时覆盖业务行为时，删除装饰断言并保留
行为断言。响应式布局只有在防止控件不可操作、内容被遮挡或超出可用屏幕时才属于行为 contract。过期测试应删除或改写，
不得以兼容层维持已经废弃的入口。

质量入口统一为 `tools/quality/run_tests.py`。`core` 用于开发期关键边界，`full` 用于完整发现；失败报告必须区分本次新增、
既有失败、缺少可选外部组件和真实环境未满足，不得用删测、放宽断言或默认跳过掩盖产品缺陷。新增或修改行为必须有对应
专项测试；纯视觉调整通过人工主题检查和真实 Windows 验收，不增加易碎像素测试。

发布准备入口为 `tools/release/prepare_release.py`。它核对统一版本、静态 dataset、本机配置、组件包、安装包和校验值，
只输出本地验证结果及维护者手工标签/发布命令，不推送、不上传，也不依赖仓库内发行说明文件。

正式发布前完成静态检查、`core`、`full`、打包输入核对、升级与回滚路径、干净安装、三种主题、账号切换、背包与角色同步、
关键计算/保存/装配、组件部署清理及真实 Windows 烟测。发布构建和安装器必须拒绝缺失、不匹配或缺少所需页面能力的
独立分析组件。已知失败须记录归属、数据影响和用户可见边界；未验证能力不得标为稳定。
